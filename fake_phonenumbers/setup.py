from setuptools import setup, find_packages

setup(
    name="phonenumbers",
    version="8.99.9",
    description="Dummy package to satisfy presidio-analyzer phonenumbers dependency. We use phonenumberslite instead.",
    packages=find_packages(),
)
